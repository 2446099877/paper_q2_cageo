# Outputs Notes

这个目录只服务新论文。

## 当前内容

- 这里已经导入了一份最小化的旧结果快照：
  - 只保留了生成论文表格所需的 `aggregated_summary.csv` 和 `aggregated_metrics.csv`
  - 用于新论文工作区内独立重建 LaTeX 表格

## 后续规则

- 新增训练一律写到这里
- 不再把新论文训练结果写回仓库根目录原来的 `outputs/`
